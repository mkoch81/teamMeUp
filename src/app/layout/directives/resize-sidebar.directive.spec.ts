import { ResizeSidebarDirective } from './resize-sidebar.directive';

describe('ResizeSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
